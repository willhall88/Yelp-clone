<%= form_for [@restaurant, @review] do |f| %>
  <%= f.label :comments %>
  <%= f.text_area :comments %>

  <%= f.label :rating %>
  <%= f.select :rating, 1..5 %>
  <%= f.submit 'Submit Review' %>

<% end %>